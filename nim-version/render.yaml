# Render deployment blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: nim-ssr
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8080
